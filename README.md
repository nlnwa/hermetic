# Hermetic

Sends data to digital storage

## Usage

### Send

```shell
hermetic send --kafka-endpoints=<list-of-kafka-endpoints> --transfer-topic <topic-name>
```

### Verify

```shell
hermetic verify --kafka-endpoints=<list-of-kafka-endpoints> --reject-topic <topic-name>
```
